// create a message to the user
const initialMessage = "You have received this message because you have been chosen to open an important vault. Here is the secret combination:";

//create 3 variables
let a, b, c;

//assign each value to the variables 
a = 2 * 5;
b = a * 4; // we use variable a to set b
c = b - 1; // we use variable b to set the value of c

//Show the message to the user with thwe combination
alert(`${initialMessage} ${a}-${b}-${c}`);
//we use template literals to concatenate easier the combination



